// Generated by CoffeeScript 1.6.3
(function() {
  var ObjectId, Schema, areaSchema, companySchema, mongoose, salesSchema, teamSchema;

  mongoose = require('mongoose');

  Schema = mongoose.Schema;

  ObjectId = Schema.Types.ObjectId;

  salesSchema = require('./salesModel').salesSchema;

  teamSchema = new Schema({
    name: String,
    leader: salesSchema
  });

  companySchema = new Schema({
    name: String,
    managers: [salesSchema],
    teams: [teamSchema]
  });

  areaSchema = new Schema({
    name: String,
    managers: [salesSchema],
    companies: [companySchema]
  });

  exports.Area = mongoose.model('Area', areaSchema);

  exports.teamSchema = teamSchema;

  exports.companySchema = companySchema;

  exports.areaSchema = areaSchema;

}).call(this);
